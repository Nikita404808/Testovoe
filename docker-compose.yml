services:
  app:
    build: .
    container_name: repair-service-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
      - DB_PATH=/app/data/app.db
      - SEED_ON_START=true
    volumes:
      - ./data:/app/data
